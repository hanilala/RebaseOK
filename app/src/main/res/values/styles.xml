<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <declare-styleable name="ProgressBarTip">
        <!-- linearGradient 进度条的开始颜色   -->
        <attr name="progressBarTip_startColor" format="color"/>
        <!-- linearGradient 进度条的结束颜色   -->
        <attr name="progressBarTip_endColor" format="color"/>
        <!-- 进度条的圆角半径 -->
        <attr name="progressBarTip_radius" format="dimension"/>
        <!-- 进度条的文字大小 -->
        <attr name="progressBarTip_txtSize" format="dimension"/>
        <!-- 进度条的文字颜色 -->
        <attr name="progressBarTip_txtColor" format="color"/>
        <!-- 进度条和文字之间的距离 -->
        <attr name="progressBarTip_distance" format="dimension"/>

    </declare-styleable>

</resources>
